name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.bun }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        # O6: CI matrix covers the minimum supported Bun + latest stable.
        # 1.1.38 is the backward-compat floor (older 1.1.x patches have a fetch
        # request-body delivery bug); the pinned dev/ship toolchain is .bun-version
        # (1.3.14), exercised here via 'latest'.
        bun: ['1.1.38', 'latest']
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ matrix.bun }}
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun test --coverage tests

  lint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint

  bench:
    name: bench (${{ matrix.channel }})
    runs-on: ubuntu-24.04
    # O5 / RT-10: per-channel benchmark gate
    strategy:
      fail-fast: false
      matrix:
        channel: [library, bunx]
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with: { bun-version: latest }
      - run: bun install --frozen-lockfile
      - run: bun run bench/harness.ts --channel=${{ matrix.channel }} --duration=30 --rps=1000
      - uses: actions/upload-artifact@v4
        with:
          name: bench-${{ matrix.channel }}-${{ github.run_id }}
          path: bench/results/

  build-binary:
    runs-on: ubuntu-24.04
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with: { bun-version: latest }
      - run: bun install --frozen-lockfile
      - run: bun run build:binary
      - uses: actions/upload-artifact@v4
        with:
          name: mockstar-binary-${{ github.sha }}
          path: dist/mockstar

  build-docker:
    # Satisfies U2 (Docker distribution channel). Build-only — publish lives in release.yml.
    runs-on: ubuntu-24.04
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: mockstar:ci-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Smoke-test the built image
        run: |
          # The hardened image is lean (examples are NOT baked in); mock content is
          # mounted at runtime, mirroring the Helm chart's ConfigMap volume pattern.
          # Default CMD serves /config/mocks.
          docker run -d --name mockstar-smoke -p 3000:3000 \
            -v "$PWD/examples/mocks:/config/mocks:ro" \
            -v "$PWD/examples/handlers:/config/handlers:ro" \
            mockstar:ci-${{ github.sha }}
          for i in 1 2 3 4 5; do
            if curl -sf http://127.0.0.1:3000/health; then exit 0; fi
            sleep 2
          done
          docker logs mockstar-smoke
          exit 1
