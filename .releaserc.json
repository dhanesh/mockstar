{
  "$schema": "https://json.schemastore.org/semantic-release.json",
  "branches": ["main"],
  "tagFormat": "v${version}",
  "_comment": "semantic-release ONLY computes the version, updates CHANGELOG + package.json, and pushes the git tag. The existing release.yml workflow (triggered by the pushed v*.*.* tag) does ALL publishing: npm, 4 binaries, multi-arch container -> ghcr.io, helm OCI, GitHub Release, cosign, SLSA. No @semantic-release/npm publish and no @semantic-release/github here, by design — release.yml owns those.",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      { "changelogFile": "CHANGELOG.md" }
    ],
    [
      "@semantic-release/npm",
      { "npmPublish": false }
    ],
    [
      "@semantic-release/exec",
      {
        "_comment": "Runs AFTER @semantic-release/npm bumps package.json: generate-schema.ts rewrites schema/mock.json ($id pinned to the minor) + schema/VERSION, and sync-version.ts rewrites src/version.ts (the CLI version constant). All committed via @semantic-release/git below so they never go stale.",
        "prepareCmd": "bun run scripts/generate-schema.ts && bun run scripts/sync-version.ts"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "CHANGELOG.md", "schema/mock.json", "schema/VERSION", "src/version.ts"],
        "_comment": "[skip ci] is safe now that publishing runs via semantic-release.yml's `publish` job (workflow_call), NOT the tag-push event. So [skip ci] only suppresses redundant push-triggered runs on the release commit (release.yml's push:tags, CI, and a self-retrigger of this workflow) — it can't block the publish.",
        "message": "chore(release): v${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}
